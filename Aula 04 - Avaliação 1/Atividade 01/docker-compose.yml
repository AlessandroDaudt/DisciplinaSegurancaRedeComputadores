name: atividade01-portal-lab

services:
  web:
    build:
      context: ./professor
    image: atividade01-portal-professor:lab
    ports:
      - "${LAB_BIND_ADDRESS:-127.0.0.1}:8443:8443"
    restart: "no"
